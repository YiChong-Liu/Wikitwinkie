services:
  accountmanagement:
    build:
      context: .
      dockerfile: accountmanagement/Dockerfile
    ports:
      - "4002:4002"
  sessions:
    build:
      context: .
      dockerfile: sessions/Dockerfile
    ports:
      - "4001:4001"
  sessionsdb:
    image: "redis:alpine"
    ports:
      - "6379:6379"
